'use strict';

System.register(['./aurelia-cookie'], function (_export, _context) {
  "use strict";

  return {
    setters: [function (_aureliaCookie) {
      var _exportObj = {};

      for (var _key in _aureliaCookie) {
        if (_key !== "default") _exportObj[_key] = _aureliaCookie[_key];
      }

      _export(_exportObj);
    }],
    execute: function () {}
  };
});